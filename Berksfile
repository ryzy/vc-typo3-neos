site :opscode

metadata

# Should be removed when original chef-chruby' is fixed
cookbook 'chruby', github: 'ryzy/chef-chruby'

cookbook 'lemp-server', github: 'ryzy/vc-lemp-server'
# cookbook 'lemp-server', path: '../vc-lemp-server'
